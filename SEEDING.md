# داده اولیه و توسعه آن

`pnpm db:seed` موضوع‌ها، واژه‌های کنترل‌شده، aliasها، ردهای نمونه و Admin محیط را به‌شکل idempotent وارد می‌کند. هر رکورد provenance و confidence دارد. `pnpm db:coverage` ماتریس JSON پوشش حرف × موضوع را در خروجی چاپ می‌کند. برای توسعه داده، آرایه‌های خوانای `prisma/data/*.json` را ویرایش و seed را دوباره اجرا کنید.

# تصمیم‌های محصول و فنی

- «ا» و «آ» در نمایش و متن canonical جدا می‌مانند، اما در تشخیص حرف آغازین عضو گروه مشترک `ا` هستند؛ بنابراین پاسخ «آبادان» در دور «ا» پذیرفته می‌شود. مدیر می‌تواند در داده‌های قواعد موضوع این رفتار را تغییر دهد.
- MVP ورود تماشاگر بعد از شروع بازی را رد می‌کند. session ناشناس ۳۰ روز اعتبار دارد و grace اتصال مجدد ۳۰ ثانیه است.
- پایان دستی ابتدا یک رزرو ۱۰ ثانیه‌ای می‌سازد. فقط نخستین درخواست معتبر رزرو می‌شود؛ قطع متوقف‌کننده یا انقضا بدون تأیید، رزرو را لغو می‌کند. تأیید نهایی همه پاسخ‌ها را قفل می‌کند.
- ناشناخته‌های موضوع باز رأی‌گیری می‌شوند. حداقل دو رأی واجد شرایط (به‌جز صاحب پاسخ) و اکثریت مطلق آرای «درست/غلط» لازم است؛ تساوی و رأی ناکافی امتیاز صفر و صف مدیر ایجاد می‌کند.
- برای جلوگیری از ادعای داده ساختگی، seed آغازین کوچک اما کنترل‌شده و دارای provenance است. ابزار idempotent import و coverage برای توسعه تا چند هزار رابطه فراهم شده است.
- احراز هویت Admin با JWT کوتاه‌عمر و bcrypt انجام می‌شود. حساب seed از متغیر محیطی ساخته می‌شود و هیچ رمز واقعی در مخزن نیست.
- state فعال اتاق برای latency و race control در حافظه server نگهداری و رخدادها/دور/پاسخ/امتیاز در PostgreSQL ثبت می‌شود. استقرار چند instance به adapter مشترک Socket.IO/Redis نیاز دارد و در محدودیت‌ها ثبت شده است.

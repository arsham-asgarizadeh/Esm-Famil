import{test,expect}from'@playwright/test';test('صفحه اصلی فارسی و قابل استفاده است',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:/اسم‌فامیل/})).toBeVisible();await expect(page.getByRole('button',{name:'ساخت اتاق'})).toBeDisabled();await page.getByPlaceholder('مثلاً سارا').fill('سارا');await expect(page.getByRole('button',{name:'ساخت اتاق'})).toBeEnabled()});

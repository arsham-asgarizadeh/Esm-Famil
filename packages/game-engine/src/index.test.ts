import {describe,expect,it} from 'vitest';import {resolveVote,scoreCategory,selectLetter,validateAnswer,type Dictionary} from './index.js';
const dictionary:Dictionary={canonicalByNormalized:new Map([['پرتقال',{id:'1',canonical:'پرتقال',categories:new Set(['fruit'])}],['پاریس',{id:'2',canonical:'پاریس',categories:new Set(['city'])}]]),aliases:new Map([['پرتغال','پرتقال']]),rejected:new Map([['پفک:fruit',{status:'WRONG_CATEGORY'}]])};
describe('validation',()=>{it('resolves aliases',()=>expect(validateAnswer({text:'پرتغال',letter:'پ',categoryId:'fruit',mode:'OPEN',dictionary}).status).toBe('VALID'));it('distinguishes open and closed',()=>{expect(validateAnswer({text:'پسته',letter:'پ',categoryId:'fruit',mode:'OPEN',dictionary}).status).toBe('UNKNOWN');expect(validateAnswer({text:'پسته',letter:'پ',categoryId:'fruit',mode:'CLOSED',dictionary}).status).toBe('WRONG_CATEGORY')});it('rejects other category',()=>expect(validateAnswer({text:'پاریس',letter:'پ',categoryId:'fruit',mode:'OPEN',dictionary}).status).toBe('WRONG_CATEGORY'))});
describe('scoring',()=>{it('gives only valid answer 20',()=>expect(scoreCategory([{playerId:'s',categoryId:'f',valid:true,canonical:'پرتقال',isStopper:false}]).get('s')).toBe(20));it('gives unique 10 and duplicates 5',()=>{const s=scoreCategory([{playerId:'s',categoryId:'f',valid:true,canonical:'پرتقال',isStopper:false},{playerId:'a',categoryId:'f',valid:true,canonical:'پرتقال',isStopper:false},{playerId:'r',categoryId:'f',valid:true,canonical:'پاپایا',isStopper:false}]);expect([...s.values()]).toEqual([5,5,10])});it('penalizes stopper invalid',()=>expect(scoreCategory([{playerId:'s',categoryId:'f',valid:false,canonical:'',isStopper:true}]).get('s')).toBe(-5))});
describe('votes and letters',()=>{it('excludes owner and requires two',()=>expect(resolveVote([{playerId:'o',choice:'VALID'},{playerId:'a',choice:'VALID'},{playerId:'b',choice:'INVALID'}],'o')).toMatchObject({accepted:false,needsAdmin:true}));it('selects covered hard letter',()=>expect(selectLetter({'پ':[10,8],'ژ':[1,1]},'HARD',[],()=>0)).toBe('ژ'))});
